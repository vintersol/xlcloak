---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - tests/fixtures/generate_fixtures.py
  - tests/fixtures/simple.xlsx
  - tests/fixtures/medium.xlsx
  - tests/fixtures/hard.xlsx
  - tests/test_fixtures.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - "Three .xlsx fixture files exist at tests/fixtures/{simple,medium,hard}.xlsx"
    - "Simple fixture has a single sheet with names, emails, phones, company names"
    - "Medium fixture has 3 sheets with cross-sheet references, Swedish PII, mixed content cells"
    - "Hard fixture has formulas, comments, merged cells, charts, multi-entity cells, edge cases"
    - "All three fixtures survive Excel I/O round-trip without text cell data loss"
    - "Hard fixture triggers surface warnings for formulas, comments, and charts"
  artifacts:
    - path: "tests/fixtures/generate_fixtures.py"
      provides: "Programmatic fixture generator — source of truth for all three .xlsx files"
      min_lines: 100
    - path: "tests/fixtures/simple.xlsx"
      provides: "Single-sheet fixture with basic PII"
    - path: "tests/fixtures/medium.xlsx"
      provides: "Multi-sheet fixture with Swedish PII and cross-sheet references"
    - path: "tests/fixtures/hard.xlsx"
      provides: "Complex fixture with unsupported surfaces and edge cases"
    - path: "tests/test_fixtures.py"
      provides: "Fixture validation and round-trip tests"
      min_lines: 60
  key_links:
    - from: "tests/fixtures/generate_fixtures.py"
      to: "openpyxl"
      via: "Creates Workbook objects and saves to .xlsx"
      pattern: "from openpyxl import Workbook"
    - from: "tests/test_fixtures.py"
      to: "src/xlcloak/excel_io.py"
      via: "Uses WorkbookReader to validate round-trip and surface detection"
      pattern: "from xlcloak\\.excel_io import WorkbookReader"
    - from: "tests/test_fixtures.py"
      to: "tests/fixtures/generate_fixtures.py"
      via: "Calls generate functions or uses generated .xlsx files"
      pattern: "fixtures/"
---

<objective>
Create three graduated test fixtures (simple/medium/hard) via a programmatic generator and validate them with tests that confirm structure, content, round-trip integrity, and surface detection.

Purpose: These fixtures serve as the validation backbone for all four phases. They graduate in complexity to exercise the full xlcloak pipeline — from basic PII replacement to conflict-aware reconciliation with unsupported surfaces. Generating them programmatically ensures reproducibility and documents the exact PII scenarios.

Output: generate_fixtures.py script, three .xlsx files committed to repo, and test_fixtures.py validating structure and round-trip.
</objective>

<execution_context>
@/home/ajans/code/xlcloak/.claude/get-shit-done/workflows/execute-plan.md
@/home/ajans/code/xlcloak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and 02 — types and classes used by fixture tests -->

From src/xlcloak/models.py:
```python
class EntityType(enum.Enum):
    PERSON = "PERSON"
    ORG = "ORG"
    EMAIL = "EMAIL"
    PHONE = "PHONE"
    URL = "URL"
    SSN_SE = "SSN_SE"
    ORGNUM_SE = "ORGNUM_SE"

@dataclass
class CellRef:
    sheet_name: str
    row: int
    col: int
    value: str | None = None

@dataclass
class SurfaceWarning:
    cell: CellRef
    surface_type: str
    detail: str
    level: str = "WARNING"
```

From src/xlcloak/excel_io.py:
```python
class WorkbookReader:
    def __init__(self, path: Path): ...
    def open(self) -> Workbook: ...
    def iter_text_cells(self, wb: Workbook) -> Iterator[CellRef]: ...
    def scan_surfaces(self, wb: Workbook) -> list[SurfaceWarning]: ...

class WorkbookWriter:
    def __init__(self, source_path: Path, output_path: Path): ...
    def patch_and_save(self, patches: list[tuple[str, int, int, str]]) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture generator script — create simple, medium, and hard .xlsx files</name>
  <files>tests/fixtures/generate_fixtures.py, tests/fixtures/simple.xlsx, tests/fixtures/medium.xlsx, tests/fixtures/hard.xlsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (fixture definitions section — exact content specs for simple/medium/hard)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-01 through D-08 — entity types that must appear in fixtures)
    - src/xlcloak/models.py (EntityType enum — all 7 types should be represented across fixtures)
  </read_first>
  <action>
Create `tests/fixtures/generate_fixtures.py` with three generator functions and a `__main__` block:

1. **`generate_simple(output_dir: Path) -> Path`** (TEST-02):
   - Create Workbook with single sheet named "Contacts"
   - Row 1 headers: Name, Email, Phone, Company, Notes
   - 20 data rows with this exact content:
     - 5 person names: "John Smith", "Maria Garcia", "Erik Andersson", "Yuki Tanaka", "Sarah O'Brien"
     - 5 emails: "john.smith@acme.com", "maria.garcia@globalcorp.net", "erik@andersson.se", "y.tanaka@example.jp", "sarah.obrien@test.org"
     - 3 phone numbers: "+1-555-0101", "+46-70-123-4567", "+81-3-1234-5678"
     - 2 company names: "Acme Corporation", "Global Technologies AB"
     - Mix non-PII text in Notes column: "Q1 review meeting", "Follow up on invoice #1234", "Budget approved for 2026", etc.
     - Some rows have all columns filled, some have gaps (None values)
   - Save to `output_dir / "simple.xlsx"`

2. **`generate_medium(output_dir: Path) -> Path`** (TEST-03):
   - Create Workbook with 3 sheets: "Contacts", "Transactions", "Summary"
   - **Contacts sheet** (50 rows):
     - Headers: Name, Email, Phone, Company, SSN, OrgNumber, Notes
     - Include the same 5 names from simple (cross-fixture consistency for testing)
     - Add 10 more unique names including Swedish names with special chars: "Bjorn Stromberg", "Anna-Karin Lindqvist"
     - 2 Swedish personnummer: "199001151234", "198507302345"
     - 1 org-nummer: "556677-8901"
     - Company names with legal suffixes: "Volvo AB", "Spotify Technology SA", "Ericsson Ltd"
     - Mixed content cells (multiple entities in one cell): "Contact Erik Andersson at erik@andersson.se", "Call +46-70-123-4567 for Volvo AB"
     - Some columns without headers (columns H, I with data but empty header row)
   - **Transactions sheet** (30 rows):
     - Headers: Date, Amount, Currency, Vendor, Contact Person, Reference
     - Numeric amounts (integers and floats) — NOT text
     - Vendor column references same companies from Contacts sheet
     - Contact Person column references same names from Contacts sheet
   - **Summary sheet** (10 rows):
     - Headers: Metric, Value
     - Mix of text and numeric values
     - One URL: "https://internal.acme.com/reports/q1"
   - Save to `output_dir / "medium.xlsx"`

3. **`generate_hard(output_dir: Path) -> Path`** (TEST-04):
   - Create Workbook with 5 sheets: "Data", "Formulas", "Formatting", "Charts", "EdgeCases"
   - **Data sheet** (30 rows):
     - Standard PII data (names, emails, phones)
     - Comments on 3 cells: `Comment("Review with John Smith", "admin")`, `Comment("PII flagged", "system")`, `Comment("Check this phone number", "reviewer")`
     - Merged cells: merge A1:C1 for a title "Employee Directory", merge A20:A22 for a department group
   - **Formulas sheet**:
     - Cell A1: numeric value 100, A2: numeric 200, A3: `=SUM(A1:A2)`
     - Cell B1: text "John Smith", B2: `=UPPER(B1)` (formula referencing text)
     - Cell C1: `=TODAY()` (date formula)
     - Cell D1: `=IF(A1>50,"High","Low")` (conditional formula)
   - **Formatting sheet**:
     - Cells with data validation: dropdown list ["Option A", "Option B", "Option C"] on column A
     - Conditional formatting on column B (not easily detectable by openpyxl scan but present in file)
   - **Charts sheet**:
     - 10 rows of numeric data in A1:B10
     - Create a BarChart from the data using `openpyxl.chart.BarChart()` and add to sheet
   - **EdgeCases sheet**:
     - Empty cell (A1 = None)
     - Very long text (A2 = 500-char string with embedded email: "Lorem ipsum ... contact admin@example.com ... dolor sit amet")
     - Unicode names: "Bjorn Stromberg" (A3), "Jose Garcia" (A4, with accent), "Li Wei" (A5, Chinese characters: "Li Wei")
     - Multi-entity cell (A6): "John Smith (john.smith@acme.com, +1-555-0101) works at Acme Corporation"
     - Special characters (A7): "O'Malley & Partners / Dept. of R&D"
     - Numeric-looking text (A8): "12345" stored as text (prefix with apostrophe or set as string)
   - Save to `output_dir / "hard.xlsx"`

4. **`if __name__ == "__main__"` block**:
   - `output_dir = Path(__file__).parent`
   - Call all three generators
   - Print paths of generated files

5. **Run the generator** after writing the script:
   - `cd /home/ajans/code/xlcloak && uv run python tests/fixtures/generate_fixtures.py`
   - Verify all three .xlsx files exist

Note on Unicode: Use ASCII approximations for names rather than actual diacritics if encoding issues arise. The key is testing non-ASCII-looking content. Use "Bjorn Stromberg" (without umlauts) and "Jose Garcia" (without accent) if needed — the important thing is the names exist, not that they have perfect Unicode.
  </action>
  <verify>
    <automated>cd /home/ajans/code/xlcloak && uv run python tests/fixtures/generate_fixtures.py && ls -la tests/fixtures/*.xlsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/generate_fixtures.py contains `def generate_simple`, `def generate_medium`, `def generate_hard`
    - tests/fixtures/generate_fixtures.py contains `if __name__ == "__main__"`
    - tests/fixtures/simple.xlsx exists and is > 0 bytes
    - tests/fixtures/medium.xlsx exists and is > 0 bytes
    - tests/fixtures/hard.xlsx exists and is > 0 bytes
    - generate_fixtures.py contains "John Smith" and "maria.garcia@globalcorp.net" (simple fixture data)
    - generate_fixtures.py contains "199001151234" (Swedish personnummer in medium fixture)
    - generate_fixtures.py contains "=SUM(A1:A2)" (formula in hard fixture)
    - generate_fixtures.py contains "BarChart" (chart creation in hard fixture)
    - generate_fixtures.py contains "Comment" import from openpyxl (comments in hard fixture)
    - `uv run python tests/fixtures/generate_fixtures.py` exits 0
  </acceptance_criteria>
  <done>Three .xlsx fixtures generated: simple (1 sheet, basic PII), medium (3 sheets, Swedish PII, cross-references), hard (5 sheets, formulas, comments, charts, merged cells, edge cases)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture validation tests — structure, content, and round-trip</name>
  <files>tests/test_fixtures.py</files>
  <read_first>
    - tests/fixtures/generate_fixtures.py (exact content of each fixture — which cells have what data)
    - src/xlcloak/excel_io.py (WorkbookReader API: open(), iter_text_cells(), scan_surfaces())
    - src/xlcloak/models.py (CellRef, SurfaceWarning dataclass fields)
  </read_first>
  <action>
Create `tests/test_fixtures.py` with these tests. Use `pathlib.Path(__file__).parent / "fixtures"` to locate fixture files.

**Simple fixture tests (TEST-02):**
- `test_simple_fixture_exists`: Assert simple.xlsx exists
- `test_simple_single_sheet`: Open with WorkbookReader, wb.sheetnames == ["Contacts"]
- `test_simple_has_headers`: First row contains "Name", "Email", "Phone", "Company", "Notes"
- `test_simple_has_pii_data`: iter_text_cells yields cells containing "John Smith", "john.smith@acme.com", "+1-555-0101"
- `test_simple_roundtrip`: Use WorkbookWriter to copy + patch one cell, read back, verify patched value and other values unchanged
- `test_simple_no_warnings`: scan_surfaces returns empty list (no formulas, comments, charts)

**Medium fixture tests (TEST-03):**
- `test_medium_fixture_exists`: Assert medium.xlsx exists
- `test_medium_three_sheets`: wb.sheetnames == ["Contacts", "Transactions", "Summary"]
- `test_medium_has_swedish_pii`: iter_text_cells yields cells containing "199001151234" (personnummer) and "556677-8901" (org-nummer)
- `test_medium_has_mixed_content_cells`: At least one cell value contains both a name and an email (contains "@" and at least two words before it)
- `test_medium_cross_sheet_names`: Same person name ("Erik Andersson") appears in text cells on both Contacts and Transactions sheets
- `test_medium_has_url`: iter_text_cells yields a cell containing "https://internal.acme.com"

**Hard fixture tests (TEST-04):**
- `test_hard_fixture_exists`: Assert hard.xlsx exists
- `test_hard_five_sheets`: wb.sheetnames has 5 entries
- `test_hard_has_formulas`: scan_surfaces returns at least one warning with surface_type="formula"
- `test_hard_has_comments`: scan_surfaces returns at least one warning with surface_type="comment"
- `test_hard_has_charts`: scan_surfaces returns at least one warning with surface_type="chart"
- `test_hard_has_merged_cells`: scan_surfaces returns at least one entry with surface_type="merged_cells"
- `test_hard_has_edge_cases`: iter_text_cells yields cells with multi-entity content (contains both "@" and "+1-555")
- `test_hard_roundtrip_preserves_formulas`: Copy hard.xlsx, read formulas sheet, cell A3 still contains "=SUM(A1:A2)" (formula string preserved)

**Cross-fixture test (TEST-01):**
- `test_all_fixtures_exist`: All three .xlsx files exist
- `test_all_fixtures_readable`: WorkbookReader can open all three without exception

All tests import from `xlcloak.excel_io` (WorkbookReader, WorkbookWriter) — do NOT import openpyxl directly in tests (test through the public API).
  </action>
  <behavior>
    - test_simple_single_sheet: sheetnames == ["Contacts"]
    - test_simple_has_pii_data: "John Smith" found in yielded text cells
    - test_hard_has_formulas: at least one SurfaceWarning with surface_type="formula"
    - test_hard_has_comments: at least one SurfaceWarning with surface_type="comment"
    - test_hard_has_charts: at least one SurfaceWarning with surface_type="chart"
    - test_all_fixtures_readable: no exceptions opening any fixture
  </behavior>
  <verify>
    <automated>cd /home/ajans/code/xlcloak && uv run pytest tests/test_fixtures.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_fixtures.py contains at least 18 test functions
    - tests/test_fixtures.py contains `test_simple_` prefix tests (at least 5)
    - tests/test_fixtures.py contains `test_medium_` prefix tests (at least 5)
    - tests/test_fixtures.py contains `test_hard_` prefix tests (at least 6)
    - tests/test_fixtures.py contains `test_all_fixtures_exist` and `test_all_fixtures_readable`
    - tests/test_fixtures.py contains `from xlcloak.excel_io import WorkbookReader`
    - `uv run pytest tests/test_fixtures.py -v` exits 0 with all tests passing
    - `uv run pytest tests/ -v --tb=short` exits 0 (full suite still green)
  </acceptance_criteria>
  <done>All fixture tests pass: structure validated, PII content confirmed, round-trip integrity proven, surface detection works on hard fixture (formulas, comments, charts, merged cells detected as warnings)</done>
</task>

</tasks>

<verification>
All fixture and full suite tests pass:
- `uv run pytest tests/test_fixtures.py -v --tb=short`
- `uv run pytest tests/ -v --tb=short` (full suite — all plans)
</verification>

<success_criteria>
1. Three .xlsx files exist at tests/fixtures/{simple,medium,hard}.xlsx
2. `uv run pytest tests/test_fixtures.py` exits 0 — all fixture tests green
3. `uv run pytest tests/` exits 0 — full suite green (Plans 01 + 02 + 03)
4. Simple fixture has 1 sheet with basic PII (names, emails, phones)
5. Medium fixture has 3 sheets with Swedish PII and cross-sheet references
6. Hard fixture triggers surface warnings for formulas, comments, charts, merged cells
7. All fixtures survive read/write round-trip without text cell data loss
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
